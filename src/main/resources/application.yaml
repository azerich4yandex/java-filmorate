logging:
  level:
    ru:
      yandex: DEBUG
    org:
      springframework:
        web: WARN
      zalando:
        logbook: TRACE
server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:file:./db/filmorate
    driverClassName: org.h2.Driver
    username: sa
    password: password
  sql:
    init:
      mode: always